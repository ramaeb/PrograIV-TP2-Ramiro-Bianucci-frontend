import { ContarCaracteres } from './contar-caracteres';

describe('ContarCaracteres', () => {
  it('should create an instance', () => {
    const directive = new ContarCaracteres();
    expect(directive).toBeTruthy();
  });
});
