import { VistaSoloAdmin } from '../directivas/vista-solo-admin';

describe('VistaSoloAdmin', () => {
  it('should create an instance', () => {
    const directive = new VistaSoloAdmin();
    expect(directive).toBeTruthy();
  });
});
