import { AppImagenDefecto } from './app-imagen-defecto';

describe('AppImagenDefecto', () => {
  it('should create an instance', () => {
    const directive = new AppImagenDefecto();
    expect(directive).toBeTruthy();
  });
});
