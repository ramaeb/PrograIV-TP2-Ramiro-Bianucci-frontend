import { PerfilNombrePipe } from './perfil-nombre-pipe';

describe('PerfilNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new PerfilNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
