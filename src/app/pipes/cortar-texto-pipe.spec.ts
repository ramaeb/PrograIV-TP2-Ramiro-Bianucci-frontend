import { CortarTextoPipe } from './cortar-texto-pipe';

describe('CortarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new CortarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
